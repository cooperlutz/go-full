# AI Startup Idea Rater
We need a single API endpoint that accepts a user prompt containing a "Startup Pitch". 
The API will pass the pitch to an LLM, returning a JSON response with a "score" (1-10) and a "critique". We also need a database table to log these scores over time.
