# app environment variables
x-app-environment: &x-app-environment
  # HTTP Server
  HTTP_PORT: ${HTTP_PORT}
  # Database
  DB_HOST: db
  DB_USER: ${DB_USER}
  DB_PASSWORD: ${DB_PASSWORD}
  DB_PORT: ${DB_PORT}
  DB_DBNAME: ${DB_DBNAME}
  DB_SSLMODE: ${DB_SSLMODE}
  DB_TYPE: ${DB_TYPE}
  # Telemetry
  OBSERVE_OTLP_HTTP_ENDPOINT: ${OBSERVE_OTLP_HTTP_ENDPOINT}
  # Security
  SEC_JWT_PRIVATE_KEY: ${SEC_JWT_PRIVATE_KEY}

services:

  app:
    container_name: go-full_app
    image: ghcr.io/cooperlutz/go-full:latest-amd64
    environment:
      <<: *x-app-environment
    depends_on:
      - db
    ports:
      - "8080:8080"
    networks:
      app_network:
        aliases:
          - app.lvh.me
